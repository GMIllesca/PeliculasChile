<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
	 <p>Listado (<%= cines.length%>):</p>
	 <% for(var i=0; i< cines.length; i++) {%>
	 	<a href="<%=cines[i].nombre%>/"><%=cines[i].nombre%></a><br>
	 <% } %>
	 <br>
	 <table border="1">
		 <thead>
			 <tr>
				 <th>Cine</th>
				 <th>Sucursales</th>
				 <th>Peliculas</th>
			 </tr>
		 </thead>
		 <tbody>
			<% for(var i=0; i< cines.length; i++) {%>
					<% for(var x=0; x< cines[i].sucursales.length; x++) {%>
						<% for(var y=0; y< cines[i].sucursales[x].peliculas.length; y++) {%>
							<tr>
									<td><%=cines[i].nombre%></td>
									<td><%=cines[i].sucursales[x].nombre%></td>
									<td><%=cines[i].sucursales[x].peliculas[y].titulo%></td>
							</tr>
						<% }%>
					<% }%>
			 <% }%>
		 </tbody>
	 </table>
  </body>
</html>
